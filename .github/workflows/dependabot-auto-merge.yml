---
name: Dependabot auto-merge

on: pull_request_target

permissions:
  contents: read
  pull-requests: read

jobs:
  dependabot:
    uses: xt0rted/.github/.github/workflows/dependabot-auto-merge.yml@main
    secrets:
      GITHUB_APP_ID: ${{ secrets.BOT_APP_ID }}
      GITHUB_APP_PRIVATE_KEY: ${{ secrets.BOT_PRIVATE_KEY }}
